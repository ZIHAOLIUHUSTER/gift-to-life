# Gift To Life 补漏修复 v2 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development`（推荐）或 `superpowers:executing-plans` 逐任务执行。本计划所有步骤使用 `- [ ]` 复选框跟踪。

**Goal:** 修复 `3af6f1c` 已构建但尚未闭环的数据导入、纯图片、手工标签、分页、AI 重试、总结去重和隐私问题，使新版可以安全发布。

**Architecture:** 保留当前 Room version 4、entryRevision、Paging 3、WorkManager 与 ZIP v2 的方向。修复不另起平行实现：导入统一走“校验 → 暂存 → Room 事务 → 文件提交”，列表只保留 Paging 一条数据流，所有用户手工修改通过 revision 失效旧 AI 任务。

**Tech Stack:** Kotlin、Room、Room `withTransaction`、Paging 3、WorkManager、SAF、`ZipFile`、JUnit4、AndroidX Room Migration Test、AndroidX Work Test。

---

## 1. 当前基线与范围

### 1.1 已验证的完成项

- `testDebugUnitTest lintDebug assembleRelease` 在 `3af6f1c` 成功。
- Release 启用 R8 和资源裁剪，`app-release-unsigned.apk` 为约 1.70 MiB。
- 数据库已升为 version 4，并注册 `MIGRATION_1_3` 与 `MIGRATION_3_4`。
- Manifest 已关闭自动备份，新增本地图标和 `releaseTest` 构建类型。

### 1.2 本计划解决的未闭环问题

| 优先级 | 问题 | 当前根因 |
|---|---|---|
| P0 | 导入失败会丢数据、可 OOM、可路径穿越 | 整文件/图片 `readBytes()`；未校验完即写最终目录；`deleteAll()` 不在事务内 |
| P1 | 纯图片无法保存 | `RecordViewModel.save()` 仍要求正文非空 |
| P1 | 手工标签仍被 AI 覆盖 | `saveWithTags()` 直接写标签后又调用会投递 AI 的 `update()` |
| P1 | Paging 未替代旧全量链路 | 旧 `getFilteredEntries()` collector 仍常驻并批量查所有标签 |
| P1 | AI 网络失败不重试 | `TagWorker` 忽略 `AiResult.RetryableFailure` 后返回 success |
| P1 | 同一周期总结可重复生成 | 生成上一周期，却用当前周期寻找 existing summary |
| P2 | 换图/删图先删文件再更新数据库 | DB 写入失败时，记录仍指向已删图片 |
| P2 | 配置导出泄露 Key、API 地址未限制 HTTPS | 直接序列化 `AppSettings`，URL 只做字符串补全 |
| P2 | 自动化覆盖不足 | JVM 仅有 SmokeTest；迁移测试未在设备执行 |

### 1.3 不在本计划内

- 不重写已存在的 Migration 架构、不增加账号/云同步、不实现多图。
- 不引入证书 pinning；仅限制 HTTPS 并提示用户信任边界。
- 不修改远端正式 `修改计划.md`；本文件是拉取最新版后的补漏计划。

---

## 2. 执行顺序

```text
Task 1 测试基线与回归夹具
  ├─ Task 2 安全原子导入
  ├─ Task 3 记录保存与手工标签
  └─ Task 4 完成 Paging 切换

Task 3 ──┬─ Task 5 AI retry 与总结去重
Task 4 ──┘

Task 2、3 ── Task 6 图片文件与配置隐私
Task 1–6 ── Task 7 设备回归与发布门禁
```

发布顺序固定为：先完成 Task 1–2 并做数据安全内部验证，再完成 Task 3–6 形成候选版本，最后由 Task 7 决定是否发布。不得先做 UI 微调或依赖升级来掩盖 P0/P1。

---

## 3. Task 1：建立能复现缺陷的测试基线

**Files:**

- Create: `app/src/test/java/com/gift/tolife/feature/record/EntrySavePolicyTest.kt`
- Create: `app/src/test/java/com/gift/tolife/core/ai/TagWorkerResultPolicyTest.kt`
- Create: `app/src/test/java/com/gift/tolife/core/common/TimeUtilTest.kt`
- Modify: `app/src/androidTest/java/com/gift/tolife/core/database/AppDatabaseMigrationTest.kt`
- Create: `app/src/androidTest/java/com/gift/tolife/core/export/ExportImportManagerTest.kt`
- Create: `app/src/androidTest/java/com/gift/tolife/core/database/EntryTransactionsTest.kt`

- [ ] **Step 1：把保存资格提取为无 Android 依赖的纯函数并先写失败测试**

新增：

```kotlin
package com.gift.tolife.feature.record

internal object EntrySavePolicy {
    fun canSave(content: String, hasImage: Boolean): Boolean {
        return content.isNotBlank() || hasImage
    }
}
```

测试必须先包含：

```kotlin
@Test fun imageOnlyEntryCanBeSaved() {
    assertTrue(EntrySavePolicy.canSave("", hasImage = true))
}

@Test fun emptyEntryCannotBeSaved() {
    assertFalse(EntrySavePolicy.canSave("", hasImage = false))
}
```

运行：

```powershell
.\gradlew.bat testDebugUnitTest --tests "*.EntrySavePolicyTest"
```

预期：在 policy 尚未接入 ViewModel 前，测试通过但现有 ViewModel 集成测试仍失败，证明要修的是调用点而不是规则本身。

- [ ] **Step 2：为 Worker 结果映射写纯单测**

新增一个只做状态映射的函数：

```kotlin
internal fun workerResultFor(result: AiResult<*>): Result = when (result) {
    is AiResult.Success -> Result.success()
    is AiResult.RetryableFailure -> Result.retry()
    is AiResult.PermanentFailure -> Result.failure()
}
```

测试覆盖 `Success`、`RetryableFailure`、`PermanentFailure`，不依赖网络或 WorkManager。

- [ ] **Step 3：修正 Migration fixture 并添加两种 version 1**

当前 `migrate1To4()` fixture 缺少真实 v1 的 `entry_tags`，而 `MIGRATION_3_4` 会为该表建索引。改为分别创建：

```sql
CREATE TABLE entry_tags (
  entryId INTEGER NOT NULL,
  tag TEXT NOT NULL,
  PRIMARY KEY(entryId, tag),
  FOREIGN KEY(entryId) REFERENCES entries(id) ON DELETE CASCADE
)
```

测试三种来源：

1. v1：没有 `imageDescription`；
2. 历史 v1：已有 `imageDescription`，但 user_version 仍为 1；
3. v3：已有 `isDeleted`、`summaryModel` 和标签。

每种都断言正文、时间、图片描述、标签、`isDeleted` 和 `entryRevision`。

- [ ] **Step 4：确认设备测试可运行**

连接真机或启动 emulator 后运行：

```powershell
adb devices
.\gradlew.bat connectedDebugAndroidTest `
  -Pandroid.testInstrumentationRunnerArguments.class=com.gift.tolife.core.database.AppDatabaseMigrationTest
```

预期：至少 3 个 migration 用例通过；没有设备时不得把 `testDebugUnitTest` 当成迁移验证。

- [ ] **Step 5：建议提交检查点**

```powershell
git add app/src/test app/src/androidTest
git commit -m "test: cover remaining data safety regressions"
```

---

## 4. Task 2：重写为安全、原子、受限的导入流程

**Files:**

- Modify: `app/src/main/java/com/gift/tolife/core/export/ExportImportManager.kt`
- Modify: `app/src/main/java/com/gift/tolife/core/export/BackupV2.kt`
- Modify: `app/src/main/java/com/gift/tolife/core/database/AppDatabase.kt`
- Modify: `app/src/main/java/com/gift/tolife/core/database/dao/EntryDao.kt`
- Modify: `app/src/main/java/com/gift/tolife/core/database/dao/EntryTagDao.kt`
- Test: `app/src/androidTest/java/com/gift/tolife/core/export/ExportImportManagerTest.kt`

- [ ] **Step 1：先写五个失败用例**

用临时 SAF URI 或 fake ContentResolver 覆盖：

1. 损坏 JSON/ZIP：旧数据库和旧图片计数不变；
2. ZIP entry `images/../../shared_prefs/gift_settings.xml`：拒绝且不写任何文件；
3. 伪造的 `imageSha256`：拒绝且旧数据不变；
4. 第二条 entry 插入抛异常：旧数据不变，暂存图片被删除；
5. 超过 `MAX_SINGLE_IMAGE_BYTES`：拒绝，不执行 `deleteAll()`。

- [ ] **Step 2：把输入先流式复制到受限临时文件**

删除：

```kotlin
val inputBytes = inputStream.readBytes()
```

替换为：

```kotlin
private fun copyToTempFile(uri: Uri): File {
    val target = File.createTempFile("gift-import-", ".bin", context.cacheDir)
    var total = 0L
    context.contentResolver.openInputStream(uri)!!.use { input ->
        target.outputStream().buffered().use { output ->
            val buffer = ByteArray(DEFAULT_BUFFER_SIZE)
            while (true) {
                val read = input.read(buffer)
                if (read < 0) break
                total += read
                check(total <= BackupV2Config.MAX_BACKUP_FILE_BYTES) {
                    "备份文件超过 2 GiB"
                }
                output.write(buffer, 0, read)
            }
        }
    }
    return target
}
```

在 `finally` 删除临时文件。不要为判断格式把内容整体载入内存；只读取前两个字节或使用 `ZipFile` 打开临时文件。

- [ ] **Step 3：先校验 ZIP，再暂存图片**

使用 `ZipFile(tempFile)`，并在任何数据库操作前执行：

```kotlin
private fun requireSafeImageEntry(name: String) {
    require(name.startsWith("images/"))
    require(!name.contains("\\"))
    require(!name.contains(".."))
    require(name.substringAfter("images/").matches(Regex("[0-9a-fA-F-]+\\.webp")))
}
```

校验规则：

- 恰有一个 `manifest.json`；
- entry 只能是 manifest 或合法图片；
- `formatVersion == 2`、`entryCount == entries.size`、条数不超过上限；
- 每个 `imageEntry` 都存在且只被一个 entry 引用；
- 流式复制每张图片时检查单张、总大小和 SHA-256；
- JSON v1 在解析后也先校验所有字段、Base64 和总大小，再进入提交阶段。

图片只写入 `cacheDir/import-<uuid>/`，不得直接写 `filesDir/images/`。

- [ ] **Step 4：使用一个 Room 事务替换数据库**

在 `ExportImportManager` 构造函数注入 `private val database: AppDatabase`，并导入 `androidx.room.withTransaction`。不得只依赖两个 DAO 分别调用，因为那无法构成替换事务。

在 `AppDatabase` 中执行：

```kotlin
suspend fun replaceAll(
    stagedEntries: List<StagedEntry>
): Int = database.withTransaction {
    entryTagDao.deleteAll()
    entryDao.deleteAll()
    stagedEntries.forEach { staged ->
        val id = entryDao.insert(staged.entry)
        entryTagDao.insertAll(staged.tags.map { EntryTag(id, it) })
    }
    stagedEntries.size
}
```

为此在 `EntryTagDao` 新增：

```kotlin
@Query("DELETE FROM entry_tags")
suspend fun deleteAll()
```

所有 entry 与标签插入必须在同一个 `withTransaction` 内；禁止在事务前调用 `entryDao.deleteAll()`。

- [ ] **Step 5：最后提交文件并处理回滚**

顺序固定：

1. 校验到暂存目录；
2. 生成新的 UUID 最终文件名；
3. 复制暂存图到 `filesDir/images/`，记录本批新增路径；
4. 执行数据库事务；
5. 事务失败：删除本批新增图片，旧 DB 与旧图片保留；
6. 事务成功：删除旧图片引用集合和暂存目录。

数据库与文件系统不能做真正跨资源事务，因此每条失败路径都必须显式清理“新文件”，且旧文件仅在 DB 成功后删除。

- [ ] **Step 6：运行导入设备测试**

```powershell
.\gradlew.bat connectedDebugAndroidTest `
  -Pandroid.testInstrumentationRunnerArguments.class=com.gift.tolife.core.export.ExportImportManagerTest
```

预期：五个失败用例都保持旧库不变；v1 与 v2 往返测试保留正文、图片、标签、删除状态和总结元数据。

- [ ] **Step 7：建议提交检查点**

```powershell
git add app/src/main/java/com/gift/tolife/core/export app/src/main/java/com/gift/tolife/core/database app/src/androidTest
git commit -m "fix: make backup import bounded and atomic"
```

---

## 5. Task 3：修复纯图片和手工标签优先级

**Files:**

- Create: `app/src/main/java/com/gift/tolife/feature/record/EntrySavePolicy.kt`
- Modify: `app/src/main/java/com/gift/tolife/feature/record/RecordViewModel.kt`
- Modify: `app/src/main/java/com/gift/tolife/feature/record/EntryComposer.kt`
- Modify: `app/src/main/java/com/gift/tolife/core/database/EntryRepository.kt`
- Modify: `app/src/main/java/com/gift/tolife/core/database/EntryTransactions.kt`
- Test: `app/src/test/java/com/gift/tolife/feature/record/EntrySavePolicyTest.kt`
- Test: `app/src/androidTest/java/com/gift/tolife/core/database/EntryTransactionsTest.kt`

- [ ] **Step 1：让 `save()` 使用统一保存资格**

```kotlin
fun save(content: String) {
    val pendingImage = _uiState.value.pendingImageUri
    if (!EntrySavePolicy.canSave(content, pendingImage != null)) return
    viewModelScope.launch {
        val imagePath = pendingImage?.let { ImageUtil.copyToPrivateDir(context, it) }
        if (pendingImage != null && imagePath == null) {
            _events.emit(RecordEvent.ShowSnackbar("图片处理失败，记录尚未保存"))
            return@launch
        }
        val entryId = repository.save(
            Entry(content = content.trim(), imagePath = imagePath, type = EntryType.NORMAL)
        )
        tagScheduler.enqueue(entryId, 0L)
        _uiState.update { it.copy(pendingImageUri = null, pendingContentText = null) }
    }
}
```

`EntryComposer` 保留 `text.isNotBlank() || pendingImageUri != null` 的 enabled 行为；UI 与 ViewModel 必须一致。

- [ ] **Step 2：手工标签通过 revision 事务保存，且不投递 AI**

`EntryRepository.setTags()` 改为只暴露：

```kotlin
suspend fun replaceUserTags(entryId: Long, tags: Set<TagType>): Long {
    return entryTransactions.replaceUserTags(entryId, tags)
}
```

删除原来逐条 `deleteByEntryId()` / `insert()` 的实现。`RecordViewModel.saveWithTags()` 必须比较正文和标签，且一个用户保存动作只推进一次 revision：

```kotlin
val original = _uiState.value.selectedEntry ?: return@launch
val oldTags = repository.getTags(entry.id).map { it.tag }.toSet()
val newTags = tags.toSet()
val contentChanged = entry.content != original.content ||
    entry.imagePath != original.imagePath

when {
    oldTags != newTags -> {
        entryTransactions.updateUserEntryAndTags(
            entry.id, entry.content, entry.imagePath, newTags
        )
        // 不 enqueue：用户本次明确选择优先
    }
    contentChanged -> {
    val revision = entryTransactions.updateUserContent(entry.id, entry.content, entry.imagePath)
    tagScheduler.enqueue(entry.id, revision)
    }
    else -> Unit
}
```

若正文未变且标签未变，直接关闭编辑器，不写 DB、不投递 Worker。若用户同时改正文和标签，`updateUserContent` 与 `replaceUserTags` 必须合并到一个 `updateUserEntryAndTags()` 事务中，并只推进一次 `entryRevision`。

- [ ] **Step 3：为“正文 + 标签同改”增加原子方法**

在 `EntryTransactions` 增加：

```kotlin
suspend fun updateUserEntryAndTags(
    id: Long,
    content: String,
    imagePath: String?,
    tags: Set<TagType>
): Long = database.withTransaction {
    check(entryDao.updateUserContent(id, content, imagePath, System.currentTimeMillis()) == 1)
    entryTagDao.deleteByEntryId(id)
    entryTagDao.insertAll(tags.map { EntryTag(id, it) })
    entryDao.getById(id)!!.entryRevision
}
```

该方法不投递 AI，因为标签是用户显式选择。

- [ ] **Step 4：验证三种标签路径**

设备测试用例：

- 仅改标签：旧 Worker 的 revision 不匹配，最终保留手工标签；
- 改正文但未改标签：投递最新 revision Worker，AI 可以更新标签；
- 同时改正文和标签：最终保留手工标签，不存在中间空标签。

- [ ] **Step 5：建议提交检查点**

```powershell
git add app/src/main/java/com/gift/tolife/feature/record app/src/main/java/com/gift/tolife/core/database app/src/test app/src/androidTest
git commit -m "fix: preserve image-only entries and manual tags"
```

---

## 6. Task 4：移除旧全量列表链路，完成 Paging 切换

**Files:**

- Modify: `app/src/main/java/com/gift/tolife/feature/record/RecordUiState.kt`
- Modify: `app/src/main/java/com/gift/tolife/feature/record/RecordViewModel.kt`
- Modify: `app/src/main/java/com/gift/tolife/feature/record/RecordScreen.kt`
- Modify: `app/src/main/java/com/gift/tolife/core/database/model/EntryListRow.kt`
- Modify: `app/src/main/java/com/gift/tolife/core/database/query/EntryQuerySqlBuilder.kt`
- Modify: `app/src/main/java/com/gift/tolife/core/database/EntryRepository.kt`
- Test: `app/src/androidTest/java/com/gift/tolife/core/database/EntryPagingTest.kt`

- [ ] **Step 1：删除旧 collector 和旧 UI 状态字段**

从 `RecordViewModel.init` 删除：

```kotlin
repository.getFilteredEntries(query)
repository.getTagsBatch(entries.map { it.id })
```

从 `RecordUiState` 删除 `entries`、`entryTags` 和仅为这两个字段服务的 `isLoading`。`EntryRepository.getFilteredEntries()` 与 `getTagsBatch()` 在没有其他调用者后删除，避免未来重新接回全量路径。

- [ ] **Step 2：Paging item 直接携带标签**

把 `EntryListRow` 作为 UI 数据，而不是立即丢掉 `tagsCsv`：

```kotlin
data class EntryListItem(
    val entry: Entry,
    val tags: List<TagType>
)
```

在 Paging `map` 中构造 `EntryListItem`，`RecordScreen` 的 `EntryCard`、预览和点击都使用 `item.tags`。这会删除 `uiState.entryTags[entry.id]` 对旧全量 Map 的依赖。

- [ ] **Step 3：编辑图片不再从全量列表查找**

`editingImageEntryId` 改为直接保存 `editingImageEntry: Entry?`；选图结果直接调用：

```kotlin
uiState.editingImageEntry?.let { entry ->
    viewModel.replaceImage(entry, uri)
}
```

不得再写 `uiState.entries.find { it.id == editingId }`。

- [ ] **Step 4：只在记录页显示 NORMAL 类型**

在 SQL 初始条件增加：

```kotlin
val where = StringBuilder("e.isDeleted = 0 AND e.type = 'NORMAL'")
```

这样总结只出现在回忆页，不混入记录时间线。

- [ ] **Step 5：加入分页压力测试**

插入 1,200 条带标签普通记录和 10 条总结，断言：

- 首次加载最多 30 条；
- 查询不会调用 `getByEntryIds`；
- 分页后仍能编辑、预览和替换图片；
- 总结不在记录页结果中；
- 标签筛选仍使用最多 4 个 tag 参数，不含 entry ID 大列表。

- [ ] **Step 6：建议提交检查点**

```powershell
git add app/src/main/java/com/gift/tolife/feature/record app/src/main/java/com/gift/tolife/core/database app/src/androidTest
git commit -m "fix: remove legacy full timeline after Paging migration"
```

---

## 7. Task 5：让 AI 重试和周期总结真正闭环

**Files:**

- Modify: `app/src/main/java/com/gift/tolife/core/ai/TagWorker.kt`
- Modify: `app/src/main/java/com/gift/tolife/feature/memory/SummaryViewModel.kt`
- Modify: `app/src/main/java/com/gift/tolife/core/common/TimeUtil.kt`
- Test: `app/src/test/java/com/gift/tolife/core/ai/TagWorkerResultPolicyTest.kt`
- Test: `app/src/test/java/com/gift/tolife/core/common/TimeUtilTest.kt`

- [ ] **Step 1：在 Worker 中显式处理每一种 `AiResult`**

视觉结果：

```kotlin
when (val vision = aiClient.describeImage(...)) {
    is AiResult.Success -> { /* 使用 vision.value */ }
    is AiResult.RetryableFailure -> return Result.retry()
    is AiResult.PermanentFailure -> {
        if (entry.content.isBlank()) return Result.failure()
        // 有正文时继续使用正文打标签
    }
}
```

标签结果：

```kotlin
return when (val tags = aiClient.chat(...)) {
    is AiResult.Success -> {
        val parsed = parseTags(tags.value).toSet()
        if (parsed.isNotEmpty()) {
            transactions.applyAiEnhancement(entryId, expectedRevision, imageDescription, parsed)
        }
        Result.success()
    }
    is AiResult.RetryableFailure -> Result.retry()
    is AiResult.PermanentFailure -> Result.failure()
}
```

revision 不匹配或已删除属于正常过期任务，返回 `Result.success()`。

- [ ] **Step 2：用上一周期查找 existing summary**

`SummaryViewModel` collector 中改为：

```kotlin
val (weekStart, weekEnd) = TimeUtil.previousWeekRange()
val (monthStart, monthEnd) = TimeUtil.previousMonthRange()
currentWeekSummary = summaries.find {
    it.summaryStart == weekStart && it.summaryEnd == weekEnd
}
currentMonthSummary = summaries.find {
    it.summaryStart == monthStart && it.summaryEnd == monthEnd
}
```

这与 `generateWeekSummary()`、`generateMonthSummary()` 使用同一范围，连续点击同一天只更新一条总结。

- [ ] **Step 3：补齐周日和跨年时间测试**

`TimeUtil` 增加接受 `Calendar` 或时间戳的内部重载，使测试能固定日期。测试：周一、周日、1 月 1 日、闰年 3 月 1 日。禁止仅依赖运行时当天。

- [ ] **Step 4：建议提交检查点**

```powershell
git add app/src/main/java/com/gift/tolife/core/ai app/src/main/java/com/gift/tolife/feature/memory app/src/main/java/com/gift/tolife/core/common app/src/test
git commit -m "fix: retry AI failures and deduplicate period summaries"
```

---

## 8. Task 6：修正图片删除顺序与配置隐私

**Files:**

- Modify: `app/src/main/java/com/gift/tolife/feature/record/RecordViewModel.kt`
- Modify: `app/src/main/java/com/gift/tolife/core/database/EntryRepository.kt`
- Modify: `app/src/main/java/com/gift/tolife/core/common/ImageStore.kt`
- Create: `app/src/main/java/com/gift/tolife/core/network/BaseUrlValidator.kt`
- Modify: `app/src/main/java/com/gift/tolife/core/network/AiClient.kt`
- Modify: `app/src/main/java/com/gift/tolife/feature/settings/SettingsViewModel.kt`
- Test: `app/src/test/java/com/gift/tolife/core/network/BaseUrlValidatorTest.kt`

- [ ] **Step 1：后删旧图，不让 DB 失败破坏现有记录**

换图与删图统一使用：

```kotlin
val oldPath = entry.imagePath
val revision = entryTransactions.updateUserContent(entry.id, content, newPath)
val deleted = imageStore.delete(oldPath)
if (!deleted && !oldPath.isNullOrBlank()) {
    _events.emit(RecordEvent.ShowSnackbar("记录已更新，旧图片将在后续清理"))
}
tagScheduler.enqueue(entry.id, revision)
```

对换图：先复制新图，DB 更新失败时删除新图；DB 成功后才删旧图。对永久清空：先在事务内得到待删路径并删除 DB，再批量删文件。软删除不删文件。

- [ ] **Step 2：增加孤儿清理入口**

在数据管理页增加“清理无主图片”。实现：查询所有 entry 的非空 `imagePath`，调用：

```kotlin
imageStore.removeOrphans(referencedPaths)
```

仅删除父目录严格等于 `filesDir/images` 的文件；显示删除数量。该入口也在导入成功后执行一次。

- [ ] **Step 3：配置导出排除 API Key**

新增：

```kotlin
data class ModelConfigExport(
    val version: Int = 2,
    val baseUrl: String,
    val tagModel: String,
    val summaryModel: String,
    val visionModel: String
)
```

`exportModelConfig()` 只能序列化这个 DTO；导入只更新 URL 和模型名，保留本机 `apiKey`。

- [ ] **Step 4：强制 HTTPS Base URL**

```kotlin
object BaseUrlValidator {
    fun normalize(raw: String): String {
        val parsed = raw.trim().trimEnd('/').toHttpUrlOrNull()
            ?: throw IllegalArgumentException("API 地址格式无效")
        require(parsed.isHttps) { "API 地址必须使用 HTTPS" }
        require(parsed.username.isEmpty() && parsed.password.isEmpty()) {
            "API 地址不能包含用户信息"
        }
        require(parsed.query == null && parsed.fragment == null) {
            "API 地址不能包含 query 或 fragment"
        }
        val base = parsed.toString().trimEnd('/')
            .removeSuffix("/v1/chat/completions")
            .trimEnd('/')
        return "$base/"
    }
}
```

设置保存和 `AiClient.getService()` 都调用 validator；无效地址在设置页显示错误且不写 SharedPreferences。

- [ ] **Step 5：建议提交检查点**

```powershell
git add app/src/main/java/com/gift/tolife/feature/record app/src/main/java/com/gift/tolife/core/common app/src/main/java/com/gift/tolife/core/network app/src/main/java/com/gift/tolife/feature/settings app/src/test
git commit -m "fix: preserve image files and protect model configuration"
```

---

## 9. Task 7：执行真实设备回归并设定发布门禁

**Files:**

- Modify: `docs/release-checklist.md`
- Modify: `README.md`
- Modify: `docs/product-intro-v2.0.md`

- [ ] **Step 1：连接设备并安装混淆测试变体**

```powershell
adb devices
.\gradlew.bat assembleReleaseTest installReleaseTest
```

预期：设备列表至少一台 `device`，而非 `offline` 或空列表；`com.gift.tolife.releasetest` 可启动。

- [ ] **Step 2：运行全部自动化验证**

```powershell
.\gradlew.bat clean testDebugUnitTest lintDebug assembleRelease connectedDebugAndroidTest
```

门禁：JVM 和 instrumentation 测试都必须有非零测试数、0 failure、0 error；Lint 必须 0 error。只运行 `SmokeTest` 不满足发布条件。

- [ ] **Step 3：完成 12 项人工回归**

1. v1 无图片描述数据库升级；
2. 历史 v1 有图片描述数据库升级；
3. v3 升级；
4. 纯文字、纯图片、图文保存；
5. 手工标签与延迟 AI 返回；
6. AI 断网、429、401；
7. 损坏 JSON、恶意 ZIP、超大图片导入；
8. v1 JSON 与 v2 ZIP 往返；
9. 1,200 条记录滚动、搜索、标签筛选、换图；
10. 周一连续生成两次上周总结；
11. 每月 1 日连续生成两次上月总结；
12. releaseTest 的 Widget、分享、导入、AI 和图片预览。

- [ ] **Step 4：更新公开文档事实**

只有回归通过后，才在 README 和产品介绍中声明：

- ZIP v2 备份是安全原子导入；
- 纯图片记录可保存；
- 10,000 条或具体容量指标；
- API Key 不会被配置导出；
- 当前 Release APK 大小。

未验证的能力不得提前写入产品介绍。

- [ ] **Step 5：建议最终提交检查点**

```powershell
git add docs README.md
git commit -m "docs: publish verified v2 behavior"
```

---

## 10. 最终完成定义

- [ ] 损坏、恶意、超限备份均不改变旧数据库和旧图片。
- [ ] v2 备份往返保留所有承诺字段；v1 备份保留其格式中存在的字段并明确报告缺失元数据，且导入内存不随图片总量线性暴涨。
- [ ] 纯图片可以保存；图片复制失败不会静默变成无图记录。
- [ ] 手工标签永远胜过旧/新 AI Worker。
- [ ] 记录页仅保留 Paging 数据链路，没有全量 collector 或 `IN (:entryIds)` 标签批查。
- [ ] 可重试 AI 错误真正进入 WorkManager retry；永久错误不无限重试。
- [ ] 一周/月只能保存一条对应上一周期的总结。
- [ ] 图片只在数据库提交成功后删除，且可清理孤儿文件。
- [ ] 配置导出不含 API Key；请求地址只能是 HTTPS。
- [ ] JVM、设备、Lint、ReleaseTest 和人工回归全部通过。

本计划只在上述完成定义满足后才建议发布；在此之前，当前版本适合继续开发验证，不适合承诺“安全恢复备份”或“已完整支持大量闪念”。
