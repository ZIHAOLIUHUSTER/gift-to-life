# 01. 产品与架构设计

## 1. 产品定位

`Gift To Life` 是一个极低负担的个人闪念记录工具。

核心目标：

- 打开即写，写完即存
- 不让用户做分类决策
- 把重点放在“回看过去”而不是“管理过去”
- AI 只做轻辅助，不成为主流程依赖

与 `Memos` 的关系：

- 借鉴其“时间流优先”的记录方式
- 借鉴其“输入器 + 列表 + 编辑弹层”的组合关系
- 借鉴其“先保存，再异步增强”的处理方式
- 不借鉴其服务端、多数据库、认证、MCP 等复杂部分

## 2. 技术路线

推荐方案：

- Kotlin
- Jetpack Compose
- Navigation Compose
- Room
- DataStore
- WorkManager
- Hilt
- Coil
- Retrofit

选择理由：

- 全部属于安卓主流官方栈，调试成本低
- AI 写 Compose / Room / ViewModel 代码的稳定性较高
- 图片、分享、后台任务、指纹锁等能力可以自然扩展
- 适合单端、本地优先、个人工具场景

## 3. 架构原则

架构原则只保留必要复杂度：

- 本地数据库是单一事实来源
- UI 只消费 ViewModel 状态，不直连数据库或网络
- AI 请求与诗词请求都属于可失败增强能力
- 所有“增强能力”失败都不能影响记录主流程

## 4. 模块划分

建议采用单 App、多 feature 的目录组织：

```text
app/
core/
  common/
  model/
  ui/
  database/
  datastore/
  network/
  ai/
feature/
  record/
  memory/
  settings/
```

职责说明：

- `app`：Application、导航容器、依赖注入入口
- `core/common`：常量、时间格式化、结果封装、通用扩展
- `core/model`：领域模型、枚举、查询参数对象
- `core/ui`：主题、颜色、通用组件、卡片样式
- `core/database`：Room Database、Entity、Dao
- `core/datastore`：设置存储
- `core/network`：Retrofit、DTO、诗词 API、AI API
- `core/ai`：提示词、AI 请求拼装、结果解析
- `feature/record`：记录页、编辑、搜索筛选、图片预览
- `feature/memory`：随机回顾、周/月总结、历史总结
- `feature/settings`：AI 参数、偏好设置、后续安全项

## 5. 核心业务流

### 5.1 保存记录

1. 用户输入文字或选择一张图片
2. 点击保存
3. 本地立即写入数据库
4. UI 立即刷新列表
5. 若配置了 API Key，则投递后台标签任务
6. 标签任务成功后回写数据库

这条链路必须满足：

- 无 AI 时仍然完整可用
- AI 失败不回滚记录
- 标签生成不阻塞用户继续写下一条

### 5.2 编辑记录

1. 点击列表项
2. 打开底部编辑面板
3. 修改文本、图片或标签
4. 保存后更新数据库
5. 列表自动刷新

### 5.3 生成总结

1. 读取指定时间范围记录
2. 不足 3 条则直接提示
3. 拼接文本发给 AI
4. 保存为特殊记录
5. 在回忆页历史总结区域展示

## 6. 数据模型

### 6.1 Entry

字段：

- `id: Long`
- `content: String`
- `imagePath: String?`
- `type: EntryType`
- `createdAt: Long`
- `updatedAt: Long`
- `summaryRange: SummaryRange?`

说明：

- 普通闪念与总结统一建模
- 总结通过 `type` 区分，不与普通标签混用

### 6.2 EntryTag

字段：

- `entryId: Long`
- `tag: TagType`

普通标签固定 4 个：

- 闪念
- 事记
- 情绪
- 知识

### 6.3 PoemCache

字段：

- `date: String`
- `content: String`
- `dynasty: String?`
- `author: String?`
- `title: String?`

### 6.4 AppSettings

通过 DataStore 存储：

- `apiKey`
- `baseUrl`
- `tagModel`
- `summaryModel`
- `biometricEnabled`

## 7. ViewModel 与状态组织

每个主页面一个 ViewModel：

- `RecordViewModel`
- `MemoryViewModel`
- `SettingsViewModel`

状态使用：

- `StateFlow<UiState>`
- 页面事件通过显式函数触发
- Snackbar / Dialog 等一次性事件单独封装

## 8. 风险控制

主要风险：

- 图片 Uri 失效
- AI 接口兼容不稳
- 总结内容过长导致超 token
- 筛选逻辑膨胀

规避策略：

- 选图后立即复制到应用私有目录
- AI 层单独封装，兼容 OpenAI 风格接口
- 总结输入设置长度上限
- 搜索筛选统一使用查询对象，不散落在 UI 层

## 9. 当前设计结论

第一阶段不做：

- 云同步
- 登录
- 多图
- 数据可视化
- 音频输入
- 知识图谱

第一阶段一定要做稳：

- 本地记录 CRUD
- 单图支持
- 搜索筛选
- 随机回顾
- AI 标签
- 周/月总结
- 设置页
