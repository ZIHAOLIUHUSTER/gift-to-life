# 02. UI 参考设计

## 1. 总体方向

这个 App 的 UI 需要参考 `Memos`，但不是把 Web 页面生硬搬到手机上。

要借鉴的是：

- 时间流卡片布局
- 页面层级清楚
- 输入器始终靠前
- 信息密度高但不压迫
- 温和、中性、安静的产品气质

不要照搬的是：

- Web 侧边栏结构
- 过多桌面端操作入口
- 偏后台管理式的设置布局

## 2. 氛围定义

目标气质：

- 温暖
- 安静
- 私人
- 轻盈

避免：

- 过强的效率工具风格
- 过于花哨的社交产品视觉
- 大面积高对比纯黑纯白
- 复杂动效

## 3. 视觉原则

### 3.1 配色

参考 `Memos` 的克制感，但在安卓上更偏纸张与日记感：

- 背景：暖白、浅米灰
- 卡片：接近纸张白，但略带暖色
- 文字：深灰而非纯黑
- 强调色：低饱和墨绿 / 灰蓝 / 棕金三选一
- 标签色：柔和，避免鲜艳荧光色

### 3.2 卡片

记录卡片应参考 `Memos` 的时间流结构：

- 中等圆角
- 极轻阴影或仅边框
- 卡片间距小而稳定
- 主要依赖留白和排版建立秩序

### 3.3 字体层级

- 页面标题：清楚但不巨大
- 正文：优先可读性，支持多行截断
- 时间：弱化显示
- 标签：小、轻、可扫读

## 4. 页面结构

App 保留 3 个主页面：

- 回忆
- 记录
- 设置

底部导航固定存在。

### 4.1 记录页

页面层级：

1. 顶部栏
2. 输入卡片
3. 记录列表
4. 悬浮随机按钮

顶部栏：

- 默认显示标题与搜索按钮
- 进入搜索模式后切换为搜索输入框

输入卡片：

- 顶部是多行输入区域
- 下方是图片缩略图预览区
- 最底部是保存按钮行

这里要参考 `Memos` 的输入器结构：

- 输入区占主导
- 功能按钮放在边缘，不要喧宾夺主
- 用户第一眼应该看到“可立即记录”

记录列表：

- 时间倒序
- 每条记录是一张卡片
- 文本预览 2 行截断
- 图片缩略图放左侧或顶部，保持统一
- 标签和时间位于信息层末端

悬浮按钮：

- 使用骰子图标
- 只在有记录时显示
- 位置靠右下，但避开系统导航手势热点

### 4.2 回忆页

页面层级：

1. 随机回顾卡片
2. 本周 / 本月总结操作区
3. 历史总结列表

随机回顾卡片：

- 始终置顶
- 像一张被重新翻出的旧纸片
- 展示完整或较大段内容
- 附带标签与时间
- 有“再抽一条”动作

总结按钮区：

- 两个主操作按钮即可
- 不要做过多说明文字
- 生成中用清楚但不夸张的加载状态

历史总结列表：

- 仍采用卡片流
- 总结卡片可以比普通记录更宽松、更像短文

### 4.3 设置页

设置页应尽量简洁：

- 按卡片分组
- AI 配置一组
- 模型配置一组
- 后续扩展功能一组

不要做成“管理后台式表单墙”。

## 5. 关键组件映射

将 `Memos` 的核心组件关系映射到安卓：

- Web 的 `MemoEditor` -> 安卓的 `EntryComposer`
- Web 的 `PagedMemoList` -> 安卓的 `EntryList`
- Web 的 `MemoView` -> 安卓的 `EntryCard`
- Web 的内联编辑/弹层编辑 -> 安卓的 `EditEntryBottomSheet`

核心要求：

- 输入器和列表独立
- 卡片和编辑器独立
- 预览图与编辑图逻辑独立

## 6. 交互原则

### 6.1 记录优先

- 打开记录页，焦点应该落在“可输入”而不是“可管理”
- 保存动作明确且稳定
- 保存后即时反馈，但不要过度弹窗

### 6.2 编辑低打扰

- 编辑使用底部弹层
- 不切换全屏编辑页
- 用户改完立即回到原上下文

### 6.3 回顾有惊喜感

- 随机回顾卡片必须是一个视觉重心
- 历史总结像私人日志，不像报表

### 6.4 AI 低存在感

- 标签生成不抢主流程
- 只在必要时提示结果
- 不要把页面做成“AI 面板”

## 7. 动效建议

只保留必要动效：

- 列表增删的轻微过渡
- BottomSheet 打开关闭动画
- 图片预览过渡
- 标签生成完成时的轻提示

不要做：

- 大段元素飞入飞出
- 复杂背景动效
- 强存在感微交互

## 8. 第一版高保真重点

如果先画 3 张关键页面，优先顺序应该是：

1. 记录页默认态
2. 记录页搜索筛选态
3. 回忆页默认态

这三张图足以定整体气质。

## 9. 当前 UI 结论

设计上要做到：

- 一眼看上去像“私人记忆工具”
- 操作上保留 `Memos` 的顺手感
- 结构上适合手机单手使用
- 情绪上安静，不像任务管理器
